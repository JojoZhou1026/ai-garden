---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="关于这里 · AI 学习小园地">
  <section class="relative">
    <div class="absolute top-0 right-0 w-96 h-96 rounded-full bg-flame/20 blur-3xl pointer-events-none"></div>
    <div class="relative max-w-3xl mx-auto px-6 py-16 md:py-20">
      <a href="/" class="inline-block text-sm text-violet hover:text-flame mb-8 transition-colors">
        ← 回到小园地
      </a>
      <p class="text-sm font-medium text-flame mb-6">✦ 关于这里</p>
      <h1 class="font-display font-extrabold text-violet text-5xl md:text-7xl leading-[0.95] tracking-tight mb-10">
        嗨，我是 Jojo。
      </h1>

      <div class="prose-jojo">
        <p>
          这里是我的 AI 学习小园地。
        </p>

        <p>
          我在外企做数字化项目 PM，原本不是技术出身。过去这段时间，我开始把 AI 真正放进自己的工作和生活里：
          搭网站、写文章、整理记忆、迁移工具、做读书助手、研究 AI Agent，也记录一些账号被封、工具切换、工作流重建这种不太体面但很真实的现场。
        </p>

        <p>
          它更像一个开放的工作台：我把自己试过、踩过、用上的东西摊出来。你路过，觉得有用，就拿走。
        </p>

        <h2>这里会有什么</h2>
        <ul>
          <li>🌱 <strong>看真事儿</strong>：记录常被问起的话题，每条都是真实经历，希望对你有点帮助。</li>
          <li>📖 <strong>扫个盲</strong>：不是官方定义，也避免专业术语。尝试用大白话，讲清那些经常听到但总有点模糊的 AI 概念。</li>
          <li>🧰 <strong>抄两招</strong>：网上的 skills 已经铺天盖地，这里只分享自己做/改造且亲测好用的。</li>
          <li>✨ <strong>瞧瞧外面</strong>：不是 AI 新闻大全。每周手动从当周的 AI 新闻里挑几条我自己感兴趣，也可能是你会感兴趣的。</li>
        </ul>

        <h2>我希望这里对谁有用</h2>
        <p>
          如果你刚开始对 AI 感兴趣，这里可以少一点术语恐吓，多一点真实入口。
        </p>
        <p>
          如果你已经在用 AI，但觉得工具、提示词、工作流有点乱，这里也许有一些能直接抄走的结构和判断标准。
        </p>
        <p>
          如果你和我一样，想把 AI 变成一种长期能力，而不是一阵新鲜感，那这里会持续记录一点工作系统、记忆管理和长期沉淀的方法。
        </p>

        <h2>这里不会有什么</h2>
        <ul>
          <li>公司、客户、内部项目和敏感信息</li>
          <li>"三分钟精通 AI"式的速成承诺</li>
          <li>为了流量硬追的热点</li>
          <li>我自己没试过、没想清楚却装懂的建议</li>
        </ul>

        <h2>更新节奏</h2>
        <p>
          不做承诺。有真实东西就写，没东西就不硬写。
        </p>

        <hr class="border-violet/20 my-12"/>

        <p class="text-violet font-display text-2xl">
          AI 不是一门要先学完才能开始用的课。
        </p>
        <p>
          它更像一种新的工作方式。一边用，一边踩坑，一边留下痕迹，慢慢就长出来了。
        </p>
        <p class="text-ink/60">
          — Jojo
        </p>
      </div>

      <section id="feedback" class="mt-16 rounded-3xl bg-cream p-6 md:p-8">
        <p class="text-xs font-medium text-flame tracking-widest uppercase mb-3">
          ✦ 留个言
        </p>
        <h2 class="font-display font-extrabold text-violet text-2xl md:text-3xl mb-2">
          想给小园地递张纸条？
        </h2>
        <p class="text-sm text-ink/65 mb-6">
          如果你对这个网站有什么建议、想看的主题、迷路的地方，或者只是想告诉我"这里还不错"，都可以写在这里。
        </p>

        <form id="site-feedback-form" class="space-y-4">
          <input
            type="text"
            name="name"
            placeholder="叫你什么？（可以不填）"
            class="w-full px-4 py-3 rounded-xl bg-peach border border-transparent focus:border-violet focus:outline-none transition-colors text-ink placeholder:text-ink/40"
            maxlength="100"
          />

          <textarea
            name="message"
            required
            rows="4"
            placeholder="比如：想看什么内容 / 哪个地方不好找 / 有什么建议……"
            class="w-full px-4 py-3 rounded-xl bg-peach border border-transparent focus:border-violet focus:outline-none transition-colors text-ink placeholder:text-ink/40 resize-none"
            maxlength="5000"
          ></textarea>

          <input
            type="text"
            name="website"
            tabindex="-1"
            autocomplete="off"
            aria-hidden="true"
            style="position:absolute; left:-9999px; width:1px; height:1px; opacity:0; pointer-events:none;"
          />

          <input type="hidden" name="post" value="关于这里" />

          <div class="flex items-center flex-wrap gap-3">
            <button
              type="submit"
              id="site-feedback-submit"
              class="px-6 py-2.5 rounded-full bg-violet text-cream text-sm font-medium hover:bg-flame transition-colors disabled:opacity-60 disabled:cursor-not-allowed"
            >
              递出去 →
            </button>
          </div>

          <p id="site-feedback-status" class="text-sm hidden" role="status" aria-live="polite"></p>
        </form>

        <script is:inline>
          (function () {
            const form = document.getElementById("site-feedback-form");
            const btn = document.getElementById("site-feedback-submit");
            const status = document.getElementById("site-feedback-status");
            if (!form || !btn || !status) return;

            form.addEventListener("submit", async (e) => {
              e.preventDefault();
              btn.disabled = true;
              btn.textContent = "递送中……";
              status.className = "text-sm hidden";

              try {
                const fd = new FormData(form);
                const res = await fetch("/api/submit", {
                  method: "POST",
                  body: fd,
                });
                const data = await res.json().catch(() => ({}));

                if (res.ok && data && data.ok) {
                  status.textContent = "收到了，谢谢你递来的纸条。";
                  status.className = "text-sm text-violet font-medium";
                  form.reset();
                } else {
                  const msg = (data && data.error) || "没递出去，可以稍后再试。";
                  status.textContent = msg;
                  status.className = "text-sm text-flame font-medium";
                }
              } catch (err) {
                status.textContent = "网络出了点问题，可以稍后再试。";
                status.className = "text-sm text-flame font-medium";
              } finally {
                btn.disabled = false;
                btn.textContent = "递出去 →";
              }
            });
          })();
        </script>
      </section>

      <div class="mt-16">
        <a href="/" class="inline-flex items-center gap-2 px-6 py-3 rounded-full bg-violet text-cream font-medium hover:bg-flame transition-colors">
          ← 回到小园地首页
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
